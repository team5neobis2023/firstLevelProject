create table question(
         id bigint primary key AUTO_INCREMENT,
         full_name varchar,
         email varchar,
         phone_number varchar,
         question varchar,
         rec_time timestamp
);
