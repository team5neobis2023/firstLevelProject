create table video_course_category(
    id bigint primary key generated by default as identity,
    name varchar(255) unique
);

ALTER TABLE video_course ADD COLUMN category_id bigint references video_course_category(id);
ALTER TABLE video_course DROP COLUMN user_id;
ALTER TABLE video_course ADD COLUMN user_id bigint references users(id);